local options = {
  ensure_installed = { "lua", "vim", "regex", "lua", "bash", "markdown", "markdown_inline", "html", "javascript", "cpp" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
